// keep requesting products at this interval while there are pending ones
export const PENDING_REQUEST_INTERVAL = 60000;

// amount of mock product cards shown while loading
export const MOCK_PRODUCTS_AMOUNT = 16;